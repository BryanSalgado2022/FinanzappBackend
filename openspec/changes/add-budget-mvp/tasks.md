## 1. Project Setup

- [x] 1.1 Scaffold FastAPI project structure (app package, routers, models, services, config)
- [x] 1.2 Add dependencies: FastAPI, SQLModel (or SQLAlchemy + Pydantic), Alembic, asyncpg/psycopg, an OAuth client library, a JWT library, pytest
- [x] 1.3 Add Docker Compose for local dev (API container + Postgres container)
- [x] 1.4 Add `.env.example` documenting required environment variables (database URL, Google OAuth client id/secret, JWT signing secret) and add `.env` to `.gitignore`
- [x] 1.5 Configure Alembic for migrations

## 2. Data Model

- [x] 2.1 Define `User` model/table (`id`, `google_sub`, `email`, `name`, `created_at`)
- [x] 2.2 Define `Concepto` model/table (`id`, `user_id`, `nombre`, `tipo` enum, `categoria` nullable, `valor_total` nullable, `activo`, `created_at`)
- [x] 2.3 Define `EntradaMensual` model/table (`id`, `concepto_id`, `anio`, `mes`, `monto_planeado`, `monto_pagado` nullable, `pagado`, unique constraint on concepto_id+anio+mes)
- [x] 2.4 Write and run initial Alembic migration creating all three tables and constraints

## 3. Authentication (spec: `auth`)

- [x] 3.1 Implement Google OAuth flow endpoint(s) (redirect/callback or token exchange, per chosen library)
- [x] 3.2 On successful OAuth callback, look up user by `google_sub`; create the user record if none exists
- [x] 3.3 Issue a backend-signed JWT on successful sign-in
- [x] 3.4 Implement a FastAPI dependency that validates the JWT and resolves the current user for protected routes
- [x] 3.5 Apply the auth dependency to all budget data routes; reject unauthenticated requests
- [x] 3.6 Ensure every concept/entry query and mutation filters by the authenticated user's id (no cross-user access)
- [x] 3.7 Write tests: first-time sign-in creates a user, returning sign-in reuses it, unauthenticated requests are rejected, user A cannot read/write user B's data

## 4. Budget Concepts (spec: `budget-concepts`)

- [x] 4.1 Implement `POST /concepts` (create), validating `tipo` is one of `deuda`/`gasto_fijo`/`ingreso`
- [x] 4.2 Implement application-layer validation: `valor_total` allowed only when `tipo = deuda`
- [x] 4.3 Implement `GET /concepts` (list, scoped to current user) and `GET /concepts/{id}`
- [x] 4.4 Implement `PATCH /concepts/{id}` (update name/category/status)
- [x] 4.5 Implement marking a concept finished (`activo = false`) and excluding finished concepts from auto-generation
- [x] 4.6 Implement `DELETE /concepts/{id}`
- [x] 4.7 Implement the remaining-balance computation for debt concepts (aggregate query: `valor_total - sum(monto_pagado)` across all monthly entries) and include it in concept read responses
- [x] 4.8 Write tests covering: create each concept type, reject invalid type, reject `valor_total` on non-debt, remaining balance across multiple years, remaining balance at zero when fully paid, list is scoped to owner, delete

## 5. Monthly Entries & Summary (spec: `monthly-budget`)

- [x] 5.1 Implement upsert logic for a single monthly entry (`monto_planeado`, `monto_pagado`, `pagado`) keyed by concepto_id+anio+mes
- [x] 5.2 Implement auto-generation: on concept creation or planned-amount edit, upsert entries for the current month through December of the current year, skipping months that already have an entry
- [x] 5.3 Implement `GET /concepts/{id}/entries` and entry update endpoint(s)
- [x] 5.4 Implement `GET /summary?year=&month=` computing `balance_neto` = sum(planned income) - sum(planned debt + fixed-expense), scoped to the current user
- [x] 5.5 Handle the no-entries-for-month case by returning `balance_neto = 0` rather than erroring
- [x] 5.6 Write tests covering: auto-generation on create, auto-generation on edit does not overwrite already-customized future months, planned vs. paid amounts differ correctly, summary balance matches the verified formula against the user's real spreadsheet numbers (July: 555,500 / August: -3,093,500)

## 6. Wrap-up

- [x] 6.1 Run the full test suite and fix any failures
- [x] 6.2 Verify the API runs end-to-end locally via Docker Compose against a fresh database (migrate, sign in, create a concept, generate entries, read summary)
- [x] 6.3 Document the API surface (OpenAPI/Swagger is auto-generated by FastAPI — confirm it's reachable and accurate)
